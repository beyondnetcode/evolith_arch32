# Compiled output
dist/

# Test coverage
coverage/

# Evolith satellite runtime artefacts generated by CLI commands
# (init, docs, agents) when they run against process.cwd() during E2E tests
.evolith/
AGENTS.md
MASTER_INDEX.md

# Jest cache
.jest-cache/

# Local env overrides
.env.local

# Bundled canonical rulesets — copied from src/rulesets at build time (copy-rulesets.js).
# Source of truth is src/rulesets; these are regenerated on prebuild/prepublishOnly.
rulesets/*
!rulesets/agents/
