# Campaign Pending Items — as of 2026-08-16

Everything the eight-week campaign has open, with what is blocking it and who can unblock it.
Companion to [the baseline](./baseline-2026-08-16.md) and [the freeze](./guard-and-gap-freeze-2026-08-16.md).

Statuses: **BLOCKED** needs something only the maintainer can do · **READY** can be picked up
now · **SCHEDULED** belongs to a later week and is not late.

---

## BLOCKED — needs the maintainer

### 1. Trigger the npm release for `core-domain@1.3.0`

The code is done and the planner confirms the shape. Nothing else in W2/W3 can proceed until
this is on the registry.

- Run `npm-release.yml` with **`dry_run=true` first**, read the log, then publish.
- The planner, run against the real registry, reports: **1 of 8 packages would publish, and it
  is `@beyondnet/evolith-core-domain@1.3.0`.** The other seven are already there and are skipped.
- Why it must go alone: HEAD's CLI imports six `core-domain` subpaths that 1.2.0 does not ship.
  Publishing the CLI first would fail its own smoke gate, correctly.

### 2. `npm deprecate` the dead `@evolith/*` scope

- `@evolith/sdk@1.0.0` is **not deprecated at all**; `@evolith/core@1.0.0` carries a message
  that names no successor. Between them ~49 installs a month go into a dead end.
- **Blocker: the local npm token cannot write.** `npm whoami` returns `beyondnetperu` and
  `npm view … maintainers` lists that account, but `GET …?write=true` returns 200 while
  `PUT https://registry.npmjs.org/@evolith%2fsdk` returns **404**, which is npm's shape for
  "no permission". Real publishes go through Actions OIDC, which is why this was never noticed.
- Fix: a token with write scope, or do the deprecate from a workflow. Creating or changing npm
  tokens is account configuration and is not something Claude can do.

---

## READY — can be picked up now

### 3. Finish the CLI English pass

The five highest-traffic commands are done: `validate`, `adr`, `init`, `agents`, `upgrade`.
A full `validate` run now contains **zero** accented characters and ends in
`❌ Validation failed. See the errors above.` What remains, by accented-literal count:

| Count | File | Note |
|---:|---|---|
| 10 | `src/commands/standards/standards.command.ts` | |
| 9 | `src/commands/architecture/scaffold.command.ts` | |
| 5 | `src/infrastructure/prompts/init-prompt-group.ts` | wizard prompts |
| 4 | `src/infrastructure/prompts/prompt.service.ts` | shared prompt chrome |
| 2 | `src/infrastructure/filesystem/file-manager.service.ts` | |
| 1 | `src/infrastructure/prompts/init-prompt-options.ts` | |

Plus, not counted above because they are not hand-edited:

- **`src/commands/api/api.catalog.tool-schemas.generated.ts` — 159 Spanish literals.** It is
  `AUTO-GENERATED by .harness/scripts/generate-capability-operations.mjs`. **Fix the generator
  and its source data, never the file** — it carries a SHA-256 of its own contents and a hand
  edit will be overwritten and will trip the drift check.
- `src/config/tool-catalog.json` — 4 Spanish prompt strings in the wizard catalogue.
- `~30` in `.test.ts` files (`observability.test.ts`, `command-executor.test.ts`). Cosmetic;
  no user ever sees them.

### 4. Regenerate the API catalogue after the generator is fixed

Downstream of item 3. `npm run gen:api-catalog`.

---

## SCHEDULED — later weeks, not late

| Week | Item |
|---|---|
| W3 | Publish CLI + MCP `1.3.0` in lockstep; tag `v1.3.0`; floating `v1` tag; cut the GitHub Release; clean-room verification in a container |
| W3 | `SECURITY.md` supported versions → `1.3.x`; delete `README.md:313` ("the npm tarballs currently published predate this hardening") |
| W4 | README restructure, "What just happened" from the W3 captured output, comparison section, `uses:` block, asciinema demo |
| W5 | Marketplace listing · MCP registry (`server.json` + `mcpName`) · `.claude-plugin/marketplace.json` + one `SKILL.md` · GHCR public |
| W6 | GO/NO-GO Monday · OpenSSF Scorecard fix + badge · 6 good-first-issues · CONTRIBUTING clone URL and build order · husky defusing for non-owners · 4 backlink PRs · OG tags + Search Console |
| W7 | Show HN, Tue 2026-09-29 or Wed 09-30, 08:00–10:00 Lima. One broadcast act, nothing else that week |
| W8 | Triage inbound <48h · read the five numbers · write the week-9 decision and the kill criterion |

---

## Claims that did not survive verification

Recorded so they are not re-adopted from the original audit. Each was measured.

| Claim | Reality |
|---|---|
| "The CLI does ~206 downloads/week" | **31/week.** The 896/month figure is release-week CI bursts — four days are >70% of the month. Target revised 400 → 150/wk. |
| "Seven manifests are missing `repository`/`homepage`" | They all have both. Only `keywords` was empty. |
| "`validate` exits 0 on failure" | **Exits 2 (BLOCKED), which is correct.** Measured: 243 checked / 111 skipped / 354 total, 96 blocking, `status: failed`, exit 2. |
| "`src/sdk/cli/.gitignore:21` excludes `policy.wasm` from npm" | `npm pack` from this checkout **includes** it, 669.4 kB. The real cause is the root `.gitignore:154` plus nothing in the publish path ever compiling it. |
| "Four chalk interpolation sites in `drift`" | **Five.** `getTrendColor` at line 184 has the same shape and was missed. |
| "Five `core-domain` subpaths the CLI needs" | **Six.** `domain/verdict` was not on the list. |
| "The published CLI is not installable (MODULE_NOT_FOUND)" — GT-625 | 1.2.2 resolves all **36** sibling specifiers and boots. That defect is fixed; the wasm one is not. |

---

## Open questions for the maintainer

1. **Push and PR?** Six commits sit on `chore/w1-visibility-foundations`, unpushed by request.
2. **`evolith.dev`** was declined for now. The `apiVersion: evolith.dev/v1` in `evolith.yaml`
   still points at a domain nobody owns, while `.io` and `.ai` are held by third parties. It
   costs ~USD 12/yr and it is the MCP registry's stronger namespace. Worth revisiting before W5.
3. **`GT-703`** stayed `PENDING` because PR #536 is open against it. Close or merge that PR and
   the row can move.
